# Core CLI dependencies
click>=8.1
rich>=13.0
plyfile>=1.0
Pillow>=10.0
numpy>=1.24
tqdm>=4.65
pyyaml>=6.0

# Training framework (includes gsplat + COLMAP wrapper)
nerfstudio>=1.1

# 3D utilities
open3d>=0.18
