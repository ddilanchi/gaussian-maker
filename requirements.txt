# Core dependencies
click>=8.1
rich>=13.0
plyfile>=1.0
open3d>=0.18
Pillow>=10.0
numpy>=1.24
tqdm>=4.65
pyyaml>=6.0

# GPU training (install separately with the right CUDA version)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# pip install gsplat
# pip install nerfstudio
